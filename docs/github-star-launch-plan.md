# GitHub Star Launch Plan

This document captures the public launch positioning for `LINE Bot Local AI Gateway Skill`. It is a repo-local checklist only; updating GitHub repository Topics, About, Releases, or Discussions still requires explicit operator approval.

## Repository Positioning

Recommended repository description:

```text
Local-first LINE AI Bot starter template with LM Studio, SQLite memory, explicit web search, and safety checks.
```

Primary audiences:

- LINE Bot + AI developers searching for LINE Bot, LINE Messaging API, webhook, or chatbot examples.
- Local LLM developers testing LM Studio, local LLMs, or OpenAI-compatible APIs.
- Privacy-conscious developers who care about local-first architecture, SQLite memory, and secret safety.

## Suggested GitHub Topics

GitHub repository topics are external repository metadata. Apply these only after explicit approval:

```text
line-bot
line-messaging-api
local-llm
lm-studio
openai-compatible
sqlite
web-search
ai-agent
chatbot
typescript
nodejs
express
privacy-first
local-first
codex-skill
starter-template
webhook
```

Keep topics lowercase and hyphenated.

## Announcement Copy

### Chinese

我開源了一個 LINE Bot Local AI Gateway Skill：

它是一個本地優先的 LINE AI Bot starter template，可以接 LM Studio / OpenAI-compatible local model server，內建 SQLite memory、明確觸發的 web search 指令，以及避免誤提交 secrets 的安全檢查。

重點不是取代 LINE Developers Console，而是讓想做本地 AI LINE Bot 的開發者，有一個安全、可檢查、可修改的起點。

GitHub：
https://github.com/OmniProtection/line-bot-local-ai-gateway-skill

### English

I open-sourced LINE Bot Local AI Gateway Skill.

It is a local-first starter template for building LINE AI bots with LM Studio / OpenAI-compatible local model servers, SQLite memory, explicit web-search commands, and safety checks to avoid leaking secrets.

It does not replace LINE Developers Console. It gives developers a safer starting point for local AI chatbot prototypes.

GitHub:
https://github.com/OmniProtection/line-bot-local-ai-gateway-skill

## First 10 Stars Checklist

- Ask 5-10 engineer friends to review the README and star it if it is useful.
- Publish a Chinese article: `如何用 LM Studio 做本地 LINE AI Bot`.
- Publish an English short post: `Local-first LINE AI bot with LM Studio`.
- Keep the README architecture diagram visible near the top.
- Open 3 focused `good first issue` items.
- Prepare `v0.1.1-alpha` release notes for README/demo improvements.
- Update the GitHub About description and docs link after approval.
- Write release notes that explain who should use it, who should not use it, and current limitations.
- Share in relevant communities with the pain point, not only the link.
- Do not buy stars or use fake accounts.
