use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "line-bot-local-searxng-dev"

search:
  formats:
    - html
    - json
  safe_search: 1
  autocomplete: ""

outgoing:
  request_timeout: 5.0
